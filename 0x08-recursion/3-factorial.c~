#include "main.h"
/**
 *factorial-a factorial function with recursion
 *@s: int input
 *
 *Return: factorial of input int n
 */
int factorial(int n)
{
if (n == 1)
{
return (1);
}
else
{
return (n * factorial(n - 1));
}
}
