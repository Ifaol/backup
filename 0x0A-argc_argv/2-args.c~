#include "main.h"
/**
 *main-a print its name function
 *@argv: input string
 *@argc: input int
 *Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
(void) argv;
printf("%d\n", (argc - 1));
return (0);
}
